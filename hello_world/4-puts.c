#include<stdio.h>

/**
 * main - return a string of characters
 * Return: 0 = SUCCESS
 */
int main(void)
{
		char string1[] = "\"Programming is like building a multilingual puzzle";

		puts(string1);
		return (0);
}
