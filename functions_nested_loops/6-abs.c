#include "main.h"
#include <stdio.h>
/**
 * _abs - calcul la valeur de l'absolue de l'integre
 * @n: int qui int
 *
 * Return: la valeur absolu de l'absol
 */
int _abs(int n)
{
	if (n < 0)
	{
	return (-n);
	}
	else
{
	return (n);
	}
}
