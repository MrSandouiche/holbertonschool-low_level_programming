#include <stdio.h>
/**
 * main - Entry point
 *
 * Description: ça imprime l'alphabet MAIS a l'envers
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char i;
	{
	for (i = 'z'; i >= 'a'; i--)
	putchar(i);
	}

return (0);
}
